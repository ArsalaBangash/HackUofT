export class Event{
    description: String;

    constructor(
        _id: String,
		name: String,
		city: String,
		country: String,
        address: String,
        startDate: Date,
        endDate: Date,
        links: String[] ) { }
}